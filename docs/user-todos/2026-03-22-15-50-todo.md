## frontend improvements

- all signup buttons should be same size
- raid signups should be divided in two groups: in and out (for any reason)
- change logic of signup buttons so that they will always look the same and require only one click to change (except cancel which has confirm)
    - this means figuring out logic for when use has not yet selected anything
- change cancel signup text to just cancel
- current design is good for mobile, but on pc there's a lot of unused space
    - better space management
    - can be different design from mobile, both are equally important
- old logo still in use in the header (the one that looks like a scorpion)
- landing page when signed in could be just the guild motd
- characters page should have portrait and maybe some more info on the cards and characters page must be paged
    - 3x3 grid with paging for pc, 3 on a page for mobile

## performance checkup

- response times
- transferred data amounts
- cost performance

## upcoming features

- guild branding
    - guild name
    - guild emblem
    - guild slogan
- access control based on guild rank
    - anyone logged in can sign up for public raids
    - guild raids cannot be created until guild master has signed in once which creates default permissions which non-permissive
        - guild master is redirected to guild settings instead of raids on first login
